# -------------------部署需要修改---------------------
bots:
  config:
    logs: false
    enable-proxy: false
    enable-bot: false


# -------------------部署需要修改---------------------

mybatis-plus:
  mapper-locations: classpath:/mapper/**.xml
  configuration:
    # MyBatis 配置
    map-underscore-to-camel-case: true

  global-config:
    # 全局配置
    banner: false
    db-config:
      table-prefix: t_
      # 数据库配置
      id-type: auto

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Qj683k2^11299%nkjb
    url: jdbc:mysql://149.28.214.142:3306/kuaisou_ai_bot?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Shanghai&useSSL=false&nullCatalogMeansCurrent=true

  # Elasticsearch 配置
  elasticsearch:
    # ES 服务器地址（支持多节点配置）
    uris:
      - http://149.28.214.142:9200
    # 认证信息
    username: elastic
    password: umEjOIAMYkf3vPq9YEaB
    # 连接超时时间（毫秒）
    connection-timeout: 5000
    # Socket 超时时间（毫秒）
    socket-timeout: 60000

  # Spring Data Elasticsearch 配置
  data:
    elasticsearch:
      repositories:
        enabled: true

logging:
  level:
    com.search.robots: info
    # ES 日志级别（生产环境建议 warn，开发环境可用 info）
    org.springframework.data.elasticsearch: info
    org.elasticsearch.client: info