# -------------------部署需要修改---------------------
bots:
  config:
    logs: false
    enable-proxy: false
    enable-bot: false


# -------------------部署需要修改---------------------

mybatis-plus:
  mapper-locations: classpath:/mapper/**.xml
  configuration:
    # MyBatis 配置
    map-underscore-to-camel-case: true
    # log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

  global-config:
    # 全局配置
    banner: false
    db-config:
      table-prefix: t_
      # 数据库配置
      id-type: auto

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Qj683k2^11299%nkjb
    url: jdbc:mysql://127.0.0.1:3306/kuaisou_ai_bot?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Shanghai&useSSL=false&nullCatalogMeansCurrent=true
    # HikariCP 连接池配置
    hikari:
      maximum-pool-size: 30           # 最大连接数30个
      minimum-idle: 5                 # 最小空闲连接5个
      connection-timeout: 30000       # 连接超时30秒
      idle-timeout: 600000            # 空闲连接超时10分钟
      max-lifetime: 1800000           # 连接最大存活30分钟
      connection-test-query: SELECT 1  # 连接测试查询
      leak-detection-threshold: 60000  # 连接泄漏检测60秒
      pool-name: CollectHikariPool    # 连接池名称
  redis:
    database: 0
    host: localhost
    port: 6379
    password: Qj683k2^11299%nkjb
    # Lettuce 连接池配置
    lettuce:
      pool:
        max-active: 20      # 最大活动连接数
        max-idle: 10        # 最大空闲连接数
        min-idle: 2         # 最小空闲连接数
        max-wait: 3000      # 最大等待时间(毫秒)
  elasticsearch:
    uris:
      - http://127.0.0.1:9200
    username: elastic
    password: umEjOIAMYkf3vPq9YEaB
    connection-timeout: 5000
    socket-timeout: 60000
  data:
    elasticsearch:
      repositories:
        enabled: true

logging:
  level:
    org.drinkless.robots: info
    org.springframework.data.elasticsearch: info
    org.elasticsearch.client: info